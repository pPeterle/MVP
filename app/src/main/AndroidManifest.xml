<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.mvp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/defaultTheme">

        <activity android:name=".ui.activity.splash.SplashActivity" android:theme="@style/splash" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity android:name=".ui.activity.home.HomeActivity" android:theme="@style/home" android:screenOrientation="portrait" />
        <activity android:name=".ui.activity.login.LoginActivity" android:theme="@style/login_register" android:screenOrientation="portrait" android:windowSoftInputMode="stateVisible|adjustResize" />

        <!-- Register -->
        <activity android:name=".ui.activity.register.RegisterActivity" android:theme="@style/login_register" android:screenOrientation="portrait" android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".ui.activity.terms.TermsActivity" />

        <activity android:name=".ui.activity.dashboard.DashboardActivity" android:theme="@style/dashboard" android:screenOrientation="portrait" android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".ui.activity.profile.ProfileActivity" android:theme="@style/defaultTheme" android:screenOrientation="portrait" />

        <activity android:name=".ui.activity.establishment.ListEstablishmentsActivity" android:theme="@style/defaultTheme" android:screenOrientation="portrait" />

    </application>


</manifest>